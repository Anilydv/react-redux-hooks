import React from "react";

export default function overviewofRedux() {
  return (
    <div>
      when we want to change a state , we dispatch an action which triggers a
      reducer function which in turn changes the state which is located in a
      global store/state
    </div>
  );
}
